yaml-version: 3.0

id: core/string-join-array
name: String Join From Array
label: 🔗
category: processing
icon: tablerLink
version: 1
style:
  header:
    background: "#6e6e6e"
  body:
    background: "#423f3f"
short_desc: This node takes an array of strings and joins them together to form a single string. 
compact: true
outputs:
  result:
    type: string
    desc: The joined string.
    index: 0
inputs:
  delimiter:
    type: string
    name: Delimiter
    required: true
    index: 0
  segments:
    name: String Segments
    type: "[]string"
    desc: The segments of the path to join.
    index: 1
